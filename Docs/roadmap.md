# S2Y iOS 可验证路线图

路线图以“什么证据能证明完成”为中心。代码合并、外部环境和真实设备是不同门槛，
不得相互替代。

## 当前基线

- H01～H27、H30～H44、H47～H56、H59～H60 已合并或在当前主题完成；当前“用户故事与 Epic 实现”目标已收口。开放工单只跟踪需要真实账号、合资格 Apple 团队或真实硬件的外部验收，不再阻塞当前目标，也不等同于已经通过验收。
- 非阻塞后续包括：付费订阅生产配置与购买验收、撤回后健康正文停止新增及真实账号删除、合资格团队签名真机、TestFlight 和真实 S2Y 设备。
- 支持的 AI 只有用户显式选择的 Apple 端侧 AI 和 Omer 在线 AI。
- 下载式 Phi/MLX 模型、模拟回答、固定人群阈值、自动健康目标和临床风险评分已退休。
- 当前闭环是健康管理闭环，不是诊断或治疗闭环。

## Gate 1：工程质量

每个 iOS Epic 合并前必须提供：

1. 一个故事一个提交，提交标题包含唯一 HLT 编号；
2. 一个主题一个 PR；
3. iOS Simulator 应用编译通过；
4. 改动相关专项测试通过；
5. 完整 `S2YTests` 通过；
6. 未通过的 CI 必须区分代码失败、外部服务失败和工具链失败；
7. 不提交密钥、生产 token、个人健康数据或本地品牌素材草稿。

## Gate 2：生产服务

H25 / US-201、US-204 的完成证据：

- Firebase Google 登录从 `www.s2y.us` 发起并成功返回；
- Firebase ID Token 在 `chat.s2y.us` 校验成功；
- Omer 对话、用量账本、订阅权限和用户数据范围可由测试账号验证；
- 云端健康数据只有在相应独立同意范围内出现；撤回后停止新增；
- Production 与 Preview 环境变量分离，不依赖 `chat-bak.s2y.us`；
- iOS Archive、签名、TestFlight 安装、启动和回滚演练完成。

## Gate 3：合资格 iPhone

H28 / US-202 的完成证据：

- Apple Foundation Models 可用状态在支持机型上正确；
- 端侧回答确实由系统模型生成，网络关闭时仍可工作；
- 首 token、总时延、取消与多轮上下文达到定义的预算；
- HealthKit 访问仍受本地权限约束；端侧对话同步另行同意；
- 不支持机型展示诚实、可恢复的不可用状态。

## Gate 4：真实 S2Y 设备

H29 / US-203 的完成证据：

- 设备身份、固件签名、协议版本和能力握手均被验证；
- 未验证设备不能启动会话；
- 用户确认后才能开始，用户停止和断连停止均在本地生效；
- 会话限制不能由 Omer 或网络响应绕过；
- 审计不保存聊天正文或不必要的健康详情；
- UI 与文档只使用健康管理/舒适度措辞，不宣称治疗效果。

## 非阻塞外部验收队列

以下工作依赖当前仓库之外的账号权限、发布资质或硬件。H60 将其从当前实现目标中解耦，
但保留原验收标准和开放工单，避免把“暂不执行”误写成“已经通过”。

### 1. 生产账号生命周期验收（P2）

仓库预检与公开端点 smoke check 已交付；继续使用生产测试账号完成 Firebase/Omer
撤回后停止新增及真实账号删除证据。不能在代码中伪造通过，也不能在没有删除授权时
操作既有生产数据。

H56 已移除发布预检对 macOS `plutil` 的依赖，隐私清单现在由 Python 标准库在 Ubuntu
和 macOS 使用相同规则校验；这只修复 CI 预检可移植性，不提供 Apple 签名凭据。

当前已确认已配对且开启 Developer Mode 的 iPhone 16e（iOS 26.6）具备 Apple
Intelligence 硬件资格，Xcode 也已登录有效个人团队；但 Debug 真机构建与签名 Archive
均已验证阻塞在个人团队不支持 HealthKit Access（Verifiable Health Records）、profile
缺少该能力，详见
[`release-validation.md`](release-validation.md)。不得通过删除临床记录 entitlement 绕过。

Omer 最新 `main` 已部署到 Production；已认证的新消息、AI 回复、刷新后读取、Free
订阅状态接口及用量聚合增长已验证。OMR-017～018 已增加版本化、分范围且可重试的
同意回执，并在新版 iOS 请求写入聊天或端侧会话前执行服务端授权门；完整回执快照可在
长期离线后恢复当前状态。OMR-020～023 已增加账号页只读范围、统一撤回和缺失
`UserConsent` 表的幂等修复迁移。H54 已由登录后的 iOS 模拟器发起最小聊天范围授权，
使用同一 Firebase 身份回查 Omer 确认状态；撤销后空授权也得到确认，随后恢复用户选择的
最小范围。H59 采用两阶段切换：Home PR #27 先发送当前策略版本并部署，随后 Omer
PR #23 强制聊天与端侧同步必须携带该版本，旧请求在计费、模型访问和数据库写入前失败
关闭；iOS 主线此前已携带该版本。健康正文停止新增与真实账号云端删除证据仍待验收。
H55 / OMR-024 已把移动端聊天删除补齐为先清理匹配的
Cognee AI 记忆、再删除数据库聊天；清理不完整时失败关闭并保留记录供重试。该路径已
部署到 Production，但只完成匿名拒绝烟测，没有删除既有生产聊天。只读元数据检查仅确认表存在性，不读取
业务行；付费订阅
映射代码已由 OMR-019 收紧：已有 Stripe 订阅不能被冲突元数据重映射，多价格项、异常
数量和未知计划均失败关闭或降级 Free；但 Production 尚无 Stripe 服务端密钥、订阅
Webhook 密钥及四个价格映射，本地留存的 Stripe 密钥也已失效，因此不能声称付费升级
可用。Home PR #25、#26 也已正常合并并部署：公开首页、多语言入口、
Omer、StVNS、企业、移动端、HOCl、使命、案例和高风险文章均已收敛到健康教育、
用户控制和产品信息边界；14 篇旧健康/设备宣称文章已设为草稿，不能把生产文案整改
误当作运行时计费或同意证据。

### 2. H28/H29 真机与硬件验收（P2）

使用测试矩阵记录机型、系统版本、固件版本、失败恢复和审计证据。

### 3. H32 跨设备选择性同步生产验收（P3）

客户端已实现按类别选择、可撤回且冲突确定的跨设备同步。仍须在生产 Firebase/Omer 与两个登录同一测试账号的设备上完成离线恢复、撤回和删除边界验收。

### 4. H33 临床文档生产边界验收（P3）

客户端已实现显式文件导入、本地解析与索引、片段引用和彻底删除；回答保持非诊断、可追溯。生产 Omer 仍须验证片段最小化、撤回后停止新增及账户删除边界。

### 5. TestFlight（延期）

当前产品决定暂不开展外测。待取得合资格 Apple 团队与所需 profile 能力并重新决定外测后，
再执行 Archive、上传、安装、回滚和审核验收；延期不阻塞当前目标，也不代表发布验收通过。

## 完成与不完成

以下情况不能标记 Epic 完成：

- 只有代码搜索没有运行验证；
- 只有模拟器结果却声称真机 AI 或 HealthKit 成功；
- 只有客户端单元测试却声称生产 Firebase/Omer 成功；
- 只有 mock 蓝牙设备却声称真实 taVNS 闭环成功；
- CI 尚未结束或失败原因未分类；
- 验收标准仍依赖未记录的人工操作。
